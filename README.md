# process-tree-alignments-cpp

Install this additional library before starting the project if you don't already have it.

On Ubuntu:
sudo apt update && sudo apt install libboost-dev

MacOs:
brew install boost

Windows:
vcpkg install boost